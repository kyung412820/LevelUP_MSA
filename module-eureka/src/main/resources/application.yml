spring:
  application:
    name: module-eurekaServer
eureka:
  server:
    enable-self-preservation: true
    evictionIntervalTimerInMs: 10000
  client:
    register-with-eureka: false
    fetch-registry: false
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8761